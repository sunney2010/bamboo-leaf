logging: 
   level: 
      # 日志级别调整
      root: info
server:
    # 服务对外暴露的端口号
   port: 8080
spring: 
  application:
    name: bamboo-server
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://www.zhuzhi.vip:3306/bamboo-leaf?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: zhuzhi2020
